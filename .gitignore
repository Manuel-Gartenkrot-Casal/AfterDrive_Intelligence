__pycache__/
spiders/__pycache__/
# Credenciales y claves — nunca subir al repo
config.json

# Python
__pycache__/
*.pyc
*.pyo
.env

# Node
express/node_modules/
express/dist/

# Data local
data/

# Backups de la base (datos, no código)
backups/

# Segundo cerebro (Obsidian) — info interna del equipo, no subir al repo
segundo-cerebro/

# Dashboard compilado: lo genera el Dockerfile (o se copia a mano desde
# express/src/public al correr en local). Versionarlo duplicaria el index.html
# y las dos copias se desincronizarian.
static/
